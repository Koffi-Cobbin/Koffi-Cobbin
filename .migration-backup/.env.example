# Base URL of the Django API (see Backend.md). Leave unset to run entirely
# against the mock data in mocks/data.ts.
NEXT_PUBLIC_API_URL=

# Shared secret with the backend for Wagtail preview → Next.js Draft Mode.
# Not wired up yet — placeholder for when Backend.md's preview flow lands.
DRAFT_MODE_SECRET=
